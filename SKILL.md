---
name: make-prd
description: 产品设计全流程流水线——从一个初步想法与资料输入，一步步输出最终的 PRD.md 文档（可交给 prd-html 直接生成交互原型）。当用户提到 PRD、需求文档、产品方案、系统设计文档、产品想法定型、需求澄清、收敛想法、审查/检查/评审 PRD 时使用，即使只说"帮我写个 PRD/方案"或"帮我看看这个 PRD/方案/需求"也要使用。
---

# make-prd — 想法到 PRD 的全流程流水线

## 定位

从一个初步想法与资料输入，经过六步流程，一步步输出最终的 **PRD.md 文档**。产物可以交给 **prd-html** skill 直接生成交互原型界面。

## 流程总览

```
STEP 0 开工确认 ──▶ STEP 1 产品定型 ──▶ STEP 2 PRD 生成 ──▶ STEP 3 质量审查 ──▶ STEP 4 修订闭环 ──▶ STEP 5 收尾交付
（想法+资料）        （定型稿★）          （PRD.md★）         （审查报告★）        （修订后PRD★）      （交付说明★）
```

每一步完成产出后，**停下来等用户确认**；用户可能要求反复修改，确认后才进入下一步。

## 贯穿规则（所有步骤遵守）

1. **每步确认**：每步产出后，在聊天中完整展示本轮改了什么、关键口径、待确认点，等用户确认后才进下一步；用户要求修改就反复改，直到确认
2. **输出即输入**：每步的 ★ 输出是下一步的输入，前后引用，不重复收集信息
3. **不猜**：信息缺失 → 写入未决事项/开放问题/`[TODO]`，等用户确认，不编造、不把假设伪装成已确认事实
4. **来源可溯**：需求、规则、范围、假设必须能追溯到用户或用户提供的材料
5. **确认在聊天里**：需要用户确认的内容完整列在回复中，不让用户自己打开文件才能评审
6. **产物落文件**：所有中间产物与最终产物都是文件，不留在聊天里

## 工作区结构（用户指定目录下）

```
<用户指定目录>/
├── 输入材料/          # 用户提供的资料（截图/文档/会议纪要等，原样归档）
└── PRD文档/           # 全部流程产物，用编号前缀文件平铺管理
```

- 目录位置由用户指定，不许自作主张
- `PRD文档/` 下所有产物采用**编号前缀文件**平铺管理，编号顺序即流程顺序（01、02、03…）
- 文件名统一为 `<项目名><迭代版本号>-NN-<文档名>.md`，如 `订单管理2.3-01-产品规划.md`
- **迭代版本号贯穿全流程**：同一版本号用于本流程全部文档，文件名与文档开头都展示（项目名、版本号、日期、状态）
- 具体产出哪些文档、各文档的形态，由对应步骤定义（见各步骤）

## STEP 0 开工确认

**目的**：正式开工前把该问的一次问清，避免中途返工。

**输入**：用户的想法描述 + 资料（截图/文档/会议纪要，可为空）。

**确认 5 项**（全部用户点头才开工）：

| # | 确认项 | 问什么 |
|---|--------|--------|
| 1 | 生成目录 | PRD 项目目录放哪？由用户指定，不许自作主张 |
| 2 | 输入材料盘点 | 有什么资料？哪些归档到 `输入材料/`？ |
| 3 | 起点状态 | 从零开始（模糊想法）还是已有定型稿/旧 PRD？（决定从 STEP 1 还是 STEP 2 进入，或跳过定型） |
| 4 | 用途确认 | 这份 PRD 最终给谁用？（内部研发 / 对外评审 / 直接出原型），影响深度和写法 |
| 5 | 迭代版本号 | 本次迭代版本号是多少（如 2.3）？贯穿本流程全部文档（文件名 + 文档开头展示） |

**动作**：确认后在指定目录下创建 `输入材料/` 与 `PRD文档/` 两个文件夹，资料归档到 `输入材料/`。

**输出**：工作区初始化完成。

**确认点**：5 项全部确认 → 进入 STEP 1。

---

## STEP 1 产品规划

**目的**：把模糊想法收敛为一份可冻结的产品规划文档（即最终定型稿），作为 STEP 2 PRD 生成的唯一事实来源。

**输入**：用户的想法描述 + `输入材料/` 中的资料。

**产出**：`<项目名><版本号>-01-产品规划.md` —— 单文档渐进填充，整个过程完成后冻结，即为最终定型稿。使用模板 [assets/01-产品规划模板.md](assets/01-产品规划模板.md) 复制结构并按节填写。

**文档结构**（按序逐节推进，节节确认）：

| # | 章节 | 内容 |
|---|------|------|
| 1 | 基本描述 | 原始需求、一句话定位、目标用户、约束、迭代记录 |
| 2 | 实现价值 | 本产品/本次迭代实现的价值——**整个方案的核心**，后面 PRD 的商业分析、收益目标、功能需求都围绕它展开：**用户价值**（对使用者）/ **业务价值**（对业务方）/ **商业价值**（对商业目标） |
| 3 | JTBD 场景 | 情境、动机、期望进展、场景优先级 |
| 4 | 范围边界 | 产品形态、MVP 范围、暂缓范围、假设 |
| 5 | 页面流程 | 页面清单、主流程、状态流转、核心数据对象、权限 |
| 6 | 开放问题 | 未决问题跟踪（阻塞/非阻塞） |

**推进方式**：

1. 逐章节填充：每节填完在聊天中展示，用户确认后进入下一节
2. 分轮提问：先问阻塞性问题（影响后续章节的），再问非阻塞问题，问题一次列全
3. 开放问题清零：所有开放问题得到答复后才可冻结
4. 冻结确认：用户确认"冻结"后，文档标记"已冻结"，成为定型稿

**输出**：★ `<项目名><版本号>-01-产品规划.md`（冻结版定型稿）—— STEP 2 的唯一事实来源。

**确认点**：逐节确认 + 冻结确认。

## STEP 2 PRD 生成

**目的**：基于冻结版产品规划，生成完整 PRD 文档（《决胜B端PRD模板v2.0》14 章裁剪为 8 核心章 + 4 可选章）。

**输入**：★ `<项目名><版本号>-01-产品规划.md`（冻结版定型稿，唯一事实来源）。

**产出**：`<项目名><版本号>-02-PRD.md` —— 单文档，含文档信息表头（产品名/迭代版本号/负责人/来源定型稿/日期/状态）与修改记录表。

**流程**：

1. **① 开工定型确认**：商业属性（商业化产品/企业自研）× 功能类型（业务型/工具型/交易型/基础服务型）1 句话确认，决定各章侧重点。适配规则见 [references/step2-prd/typing.md](references/step2-prd/typing.md)
2. **② 文档初始化**：创建 `<项目名><版本号>-02-PRD.md`，从模板 [assets/02-PRD模板.md](assets/02-PRD模板.md) 复制骨架（文档信息表头 + 修改记录表 + 章节框架），展示给用户确认
3. **③ 前置章节（核心 1-4 章）**：逐章生成、逐章聊天确认；信息来自 01-产品规划，不重复收集。各章生成指引见 [references/step2-prd/](references/step2-prd/)（ch01-ch08、ca/cb/cd、typing、selfcheck）
4. **④ 模块规划先行（第 5 章 10.0）**：根据页面流程+功能清单产出模块规划（模块清单/页面菜单功能覆盖/依赖图/建议开发顺序/可并行）→ 聊天完整展示 → **用户确认后才写功能详情**
5. **⑤ 第 5 章功能需求**：10.1 产品框架概述（Mermaid 应用架构图/ER 数据模型/业务流程图/状态机 + 功能清单）→ 10.2 逐模块详情 → 10.3 异常情况处理。**逐模块分步输出**：每完成一个功能模块（7 段式）即暂停，聊天完整展示该模块，用户确认后才继续写下一个模块，不自动连续输出
6. **⑥ 核心 6-8 章**：数据埋点 / 角色与权限 / 待决事项（写作全程待决策汇总）
7. **⑦ 可选章提示确认**：8 个核心章全部完成后，在聊天中列出可选章 A-D，由用户确认需要哪些 → 有则继续写选中的章 → 全部不需要则进入下一步
8. **⑧ 自检与缺口分析**：自检清单见 [references/step2-prd/selfcheck.md](references/step2-prd/selfcheck.md)；[TODO] 项汇总进第 8 章待决事项

**修改记录纪律**：对 PRD 的任何修改（逐章/逐模块确认后的追加、用户中途提出的调整、后续步骤的修订）都在文档头部修改记录表**追加一条记录**（时间/变更单元/动作/评审状态），只追加、不改写旧记录。

**断点续写**：若会话中断，输出断点摘要（已完成章节/待完成章节/未决问题），下次继续时先恢复断点再续写。

**确认点**：定型确认、逐章确认、模块规划确认、逐模块确认、可选章确认、自检清单确认。

**输出**：★ `<项目名><版本号>-02-PRD.md`（完整 PRD，含修改记录表）—— STEP 3 审查的对象。

## STEP 3 质量审查

**目的**：对 STEP 2 产出的 PRD 进行 13 维度质量审查，输出质量审查报告，为 STEP 4 修订闭环提供问题清单。

**输入**：★ `<项目名><版本号>-02-PRD.md`（STEP 2 输出）。

**触发**：STEP 2 完成后，提示用户进入 STEP 3；用户确认后触发审查。

**产出**：`<项目名><版本号>-03-质量审查报告.md`（与 01/02 同目录）+ 聊天展示摘要。

**审查维度（13 个：12 常驻 + d13 AI 按需）**：

| 编号 | 维度 | 说明 |
|------|------|------|
| 01 | 业务分析与场景 | 原业务分析+场景分析合并 |
| 02 | 定位与商业分析 | 原产品定位+商业分析深度合并；商业深度（2.5 节）仅商业化产品适用 |
| 03 | MVP策略与演进 | 原 MVP |
| 04 | 架构设计质量 | |
| 05 | 数据建模质量 | |
| 06 | 流程与角色设计 | |
| 07 | 交互设计质量 | |
| 08 | 异常处理与健壮性 | |
| 09 | 数据埋点 | 与 PRD 第 6 章结构一致，独立一章 |
| 10 | 运营与效果跟踪 | |
| 11 | 文档结构完整性 | 按 8 核心章 + 4 可选章核对 |
| 12 | 产品定型适配 | 商业属性 × 功能类型 |
| 13 | AI 功能设计（按需） | PRD 含 AI 功能时才检查，不占常驻流程 |

各维度检查清单见 [references/step3-review/](references/step3-review/)（d01-d13 文件 + guide + veto）。

**流程**：

1. **① 审查前确认**：确认 PRD 类型（新产品/迭代/模块）× 产品属性（自研/SaaS/项目制），按 [references/step3-review/guide.md](references/step3-review/guide.md) 2.1 确定维度侧重
2. **② 快速浏览**：通读 PRD 全文，获取整体印象，标注第一印象明显问题
3. **③ 重大风险项快速排查**：逐一检查 R1-R8（见 [references/step3-review/veto.md](references/step3-review/veto.md)）。**命中记录不打断**——继续完成全部维度检查，风险项记录在报告【三、重大风险项】区并标注"建议阻塞推进"，最终是否打回 STEP 2 由用户决定
4. **④ 13 维度逐一检查**：按编号顺序执行各维度检查清单，每个检查项标注"发现问题/未发现问题/不适用"；发现问题记录：编号（P0-P3 分级）/描述/所属维度/影响范围/改进建议/参考依据
5. **⑤ 汇总报告**：按 [references/step3-review/guide.md](references/step3-review/guide.md) 报告模板生成 `<项目名><版本号>-03-质量审查报告.md`（封面/产品定型说明/维度发现摘要/重大风险项/问题清单 P0-P3/亮点记录/Top 10 改进建议/综合建议——不做通过/不通过判定）；聊天中展示摘要：P0/P1 问题全量、P2/P3 汇总、是否命中重大风险、综合建议核心结论

**确认点**：审查前确认、报告确认（用户确认报告后进入 STEP 4）。

**输出**：★ `<项目名><版本号>-03-质量审查报告.md` —— STEP 4 修订的依据（问题清单）。

## STEP 4 修订闭环

**目的**：按 03-质量审查报告 的问题清单逐条修订 PRD，形成"问题清单 → 修订 → 复查"闭环。

**输入**：★ `<项目名><版本号>-02-PRD.md` + ★ `<项目名><版本号>-03-质量审查报告.md`。

**触发**：STEP 3 报告确认后进入。若报告标注"建议阻塞推进"的重大风险且用户决定打回，则先回 STEP 2 修订，重新 STEP 3 审查后再进入本步。

**修订流程**：

1. **① 分批修订**：按优先级分批——先 P0/P1 批，后 P2/P3 批。每批内**逐条确认修改方案**：展示该问题（编号/描述/所属维度/影响范围）+ 建议的修改方案（改什么、怎么改），用户确认"改 / 不改 / 改法调整"后才执行
2. **② 逐条修改执行**：确认后修改 PRD 对应章节；按 STEP 2 修改记录纪律在头部修改记录表**追加记录**（时间/变更单元/动作/评审状态），只追加不改写
3. **③ 修订同步**：修订过程中新增的未决事项 → 追加进第 8 章待决事项（D 编号）；因修订已解决的待决事项同步更新状态
4. **④ 复查闭环**：全部批次修订完成后，聊天中逐条输出复查清单（每条问题：已修复 / 部分修复 / 未修复，附修复后内容位置），用户确认后进入 STEP 5；复查记录**追加到 03-质量审查报告.md 末尾**，形成闭环留痕

**确认点**：每条修改方案确认、复查清单确认。

**输出**：★ 修订后 `<项目名><版本号>-02-PRD.md`（含修改记录追加）+ 03-质量审查报告.md 复查记录 —— STEP 5 收尾交付的对象。

## STEP 5 收尾交付

**目的**：全套流程产物收尾交付，生成交付说明，提供下一步建议。

**输入**：★ 修订后 `<项目名><版本号>-02-PRD.md` + 01-产品规划 + 03-质量审查报告（含复查记录）。

**产出**：`<项目名><版本号>-04-交付说明.md` + 聊天展示交付摘要。

**流程**：

1. **① 交付清单核对**：核对全套产物（01 规划 / 02 PRD / 03 报告 / 04 说明），确认 PRD 状态、版本号、遗留事项
2. **② 生成交付说明**：`<项目名><版本号>-04-交付说明.md` —— 交付物清单（文档 + 用途 + 状态）、遗留事项（未修复问题 / 待决事项 D 编号）、版本与状态（迭代版本号、PRD 状态：已交付）、下一步建议（对接 prd-html 生成原型 / 评审会等）
3. **③ 交付摘要确认**：聊天中展示交付摘要（交付物清单 / 遗留事项 / 下一步建议），用户确认后流程完成

**确认点**：交付摘要确认。

**输出**：★ 全套交付产物（01 / 02 / 03 / 04 编号文件）——流程完成，产物可交 prd-html skill 生成原型界面。
